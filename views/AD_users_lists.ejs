<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Document</title>
</head>
<body>
<%- include ('header/header.ejs') %>
<div class="container mb-4">
    <div>
        <h1>Edit Members</h1>
    </div>

    <div>
        <table class="table table-bordered table-hover">
            <thead class="table-dark">
                <tr>
                    <th>Username</th>
                    <th>Password</th>
                    <th>Role</th>
                    <th>Change Role</th>
                    <th>Delete</th>
                </tr>
            </thead>


            <tbody>
                <% user.forEach(user=> { %>
                    
                    <tr>
                        <td>
                            <form action="/admin/changeName/<%= user.user_id %>">
                            <input type="text" id="user" name="username" class="form-control" placeholder="Username" value="<%= user.username %>" required>
                            <button type="submit" class="btn btn-success btn-sm mt-2">Update</button>
                            </form>
                        </td>

                        <td>
                            <form action="/admin/changePassword/<%= user.user_id %>">
                            <input type="password" id="password" name="password" class="form-control" placeholder="password" required>
                            <button type="submit" class="btn btn-success btn-sm mt-2">Update</button>
                            </form>
                        </td>

                        <td>
                            <% if (user.role === "admin") { %>
                                <h5>Admin</h5>
                            <% } else if (user.role === "group_owner") {%>
                                <h5>Group Owner</h5>
                            <% } else if (user.role === "group_member") {%>
                                <h5>Group Member</h5>
                            <% } else if (user.role === "normal_user") {%>
                                <h5>Normal User</h5>
                            <% } %>
                        </td>

                        <td>
                            <form action="/admin/changeRole/<%= user.user_id %>" method="POST">
                            <select name="role" class="form-select">
                                <option value="admin" <%= user.role==="admin" ?"selected ":"" %>>
                                    Admin
                                </option>
                                <option value="group_owner" <%= user.role==="group_owner" ?"selected disabled":"" %> >
                                    Group Owner
                                </option>
                                <option value="group_member" <%= user.role==="group_member" ?"selected disabled":"" %> >
                                    Group Member
                                </option>
                                <option value="normal_user" <%= user.role==="normal_user" ?"selected ":"" %>>
                                    Normal User
                                </option>
                            </select>
                            <button type="submit" class="btn btn-success btn-sm mt-2">Update</button>
                            </form>
                        </td>
                        
                        <td>
                            <form action="/admin/deleteUser" method="POST">
                                <input type="hidden" name="user_id" value="<%= user.user_id %>">
                                <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Delete this user?')"> Delete </button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>

            </tbody>
        </table>
    </div>
</div>

</body>
</html>